import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#2E3DA3' },
    secondary: { main: '#8996EB' },
  },
  typography: {
    fontFamily: '"Montserrat", sans-serif',
  },
});

export default theme;
